if [ -d .taskboard-env ];
then
        source .taskboard-env/bin/activate
        pip install -r requirements.txt
else
        python -m venv .taskboard-env
        source .taskboard-env/bin/activate
        pip install -r requirements.txt
fi